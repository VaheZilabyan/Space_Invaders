#ifndef GLOBALS_HPP
#define GLOBALS_HPP       

const int X = 21;
const int Y = 51;

extern int score;
extern int life;
extern int k; //erb verevic krakac patrony hasnum e nerqev avelanum e mekov
extern bool t;//erb krakum enq t==true
extern int gg;
extern bool newgame;
extern bool end; //durs galu hamar
extern bool is_not_empty;// (if == true)-> not empty ;else empty 
extern bool deleting_record;//if == true -> nothing to delete;else records will be deleted

#endif 
